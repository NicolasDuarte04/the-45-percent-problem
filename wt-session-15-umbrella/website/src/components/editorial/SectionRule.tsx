/**
 * §2.8: centered · · · ornament rendered between H2 breaks on editorial
 * surfaces. Decorative only.
 */
export function SectionRule() {
  return <div className="vault-section-rule" aria-hidden />;
}
