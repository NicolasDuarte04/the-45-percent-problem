"""Session 15 gate-bite proof — TEMPORARY, reverted in the next commit.

Deliberately failing test to demonstrate voto_ci.yml goes red on a 21J
engine regression. Must NOT survive to merge.
"""


def test_gate_bites():
    assert False, "deliberate failure: proving voto_ci.yml bites"
