# pnpm v10+ requires `packages:` to be present whenever pnpm-workspace.yaml
# exists. The website is a single-package "workspace" — declaring "." keeps
# the existing settings (ignoredBuiltDependencies) loadable without changing
# install topology.
packages:
  - "."

ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
