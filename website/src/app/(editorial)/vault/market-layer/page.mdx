# Market Layer

This article is in preparation. It will explain how raw bookmaker decimal odds are converted into de-vigged implied probabilities using the power method, why the simpler proportional method introduces a systematic bias, and how the edge metric E is computed and interpreted.

An interactive figure illustrating the divergence between the two de-vigging methods will be embedded here.

<SectionRule />
